<IfModule mod_rewrite.c>
  RewriteEngine on
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>


# Disable gzip and buffering
SetEnv no-gzip 1
SetEnv dont-vary 1

SetEnvIfNoCase Request_URI ".*" no-gzip